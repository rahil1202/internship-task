

# 🧠 **DAY 3 — PART 4 — STRINGS (MEGA DEEP DIVE)**

A complete guide—simple explanations + full method breakdowns + real life uses.

---

# 🎯 **4.1 — What Is a String?**

A string is text inside quotes.

```js
let name = "Rahil";
let city = 'Mumbai';
let bio = `JavaScript developer`;
```

Strings are **primitive** and **immutable** (cannot be changed once created).

---

# 🎭 **4.2 — How to Create Strings**

### 1. String Literal (most used)

```js
let s = "Hello";
```

### 2. String Constructor (rare, usually avoid)

```js
let s = new String("Hello");
```

### 3. Template Literals (backticks)

```js
let user = "Rahil";
let msg = `Hello ${user}`;
```

Supports variables, expressions, and multi-lines.

---

# 🌋 **4.3 — String Indexing**

Works like an array of characters:

```js
let s = "Rahil";
console.log(s[0]); // R
console.log(s[4]); // l
```

You cannot change characters:

```js
s[0] = "X"; // ❌ ignored
```

---

# ✂️ **4.4 — Length**

```js
"apple".length; // 5
```

---

# 🧱 **4.5 — Extracting Substrings**

## ✔ slice(start, end)

Supports negative indices.

```js
"JavaScript".slice(0, 4);   // Java
"JavaScript".slice(-6);     // Script
"JavaScript".slice(-6, -3); // Scr
```

---

## ✔ substring(start, end)

Does NOT support negative values and auto-swaps.

```js
"hello".substring(1, 4); // ell
"hello".substring(4, 1); // ell (swapped)
```

---

## ✔ substr(start, length) — outdated

```js
"developer".substr(0, 5); // devel
```

---

# 🔍 **4.6 — Searching in Strings**

## ✔ indexOf()

```js
"frontend".indexOf("end"); // 4
```

## ✔ lastIndexOf()

```js
"banana".lastIndexOf("a"); // 5
```

## ✔ includes()

```js
"hello world".includes("world"); // true
```

## ✔ startsWith() & endsWith()

```js
"Rahil".startsWith("Ra"); // true
"Rahil".endsWith("il");   // true
```

---

# 🔧 **4.7 — Modifying Strings (Remember: strings are immutable!)**

## ✔ replace()

Replaces first match:

```js
"ha ha ha".replace("ha", "he"); // he ha ha
```

Regex version for all matches:

```js
"ha ha ha".replace(/ha/g, "he"); // he he he
```

---

## ✔ replaceAll()

```js
"ha ha ha".replaceAll("ha", "he"); // he he he
```

---

# 🧽 **4.8 — Trimming**

```js
"   Rahil   ".trim();      // "Rahil"
"   Rahil".trimStart();    // "Rahil"
"Rahil   ".trimEnd();      // "Rahil"
```

---

# 🔡 **4.9 — Case Changing**

```js
"rahil".toUpperCase(); // RAHIL
"HELLO".toLowerCase(); // hello
```

---

# 🔗 **4.10 — Concatenation**

```js
"Hello".concat(" ", "Rahil"); // Hello Rahil
```

Usually better:

```js
"Hello " + "Rahil";
```

---

# 🔥 **4.11 — Split (String → Array)**

```js
"apple,banana,mango".split(",");
// ["apple", "banana", "mango"]

"Rahil".split("");
// ["R","a","h","i","l"]
```

Limit:

```js
"a b c d".split(" ", 2); // ["a", "b"]
```

---

# 🎨 **4.12 — repeat()**

```js
"*".repeat(5); // *****
```

---

# 🧾 **4.13 — padStart() & padEnd()**

Useful for formatting IDs, numbers, timestamps.

```js
"5".padStart(3, "0"); // 005
"JS".padEnd(5, "_");  // JS___
```

---

# 🧬 **4.14 — charAt(), charCodeAt(), fromCharCode()**

### charAt()

```js
"hello".charAt(1); // e
```

### charCodeAt()

```js
"A".charCodeAt(0); // 65
```

### fromCharCode()

```js
String.fromCharCode(65); // A
```

---

# 🧠 **4.15 — search() (Regex Search)**

```js
"hello123".search(/\d/); // 5
```

---

# 🎯 **4.16 — match() & matchAll() (Regex Power)**

### match()

```js
"123abc456".match(/\d+/g);
// ["123", "456"]
```

### matchAll()

```js
for (let m of "test1test2".matchAll(/test/g)) {
  console.log(m.index);
}
// 0
// 5
```

---

# 🪞 **4.17 — localeCompare()**

Used for alphabetical sorting.

```js
"apple".localeCompare("banana"); // -1
```

---

# 🧩 **4.18 — normalize() (Unicode Handling)**

Used for international characters.

```js
"é".normalize();
```

---

# ♻️ **4.19 — toString() & valueOf()**

```js
(123).toString();       // "123"
new String("Rahil").valueOf(); // "Rahil"
```

---

# 🧱 **4.20 — Looping Through Strings**

```js
for (let char of "Rahil") {
  console.log(char);
}
```

---

# 🎒 **4.21 — Real Life String Use-Cases**

---

## 📌 Extract email domain

```js
function getDomain(email) {
  return email.slice(email.indexOf("@") + 1);
}
```

---

## 📌 Capitalize first letter

```js
function cap(s) {
  return s[0].toUpperCase() + s.slice(1);
}
```

---

## 📌 Slugify text (URL-friendly)

```js
function slugify(str) {
  return str
    .trim()
    .toLowerCase()
    .replaceAll(" ", "-")
    .replace(/[^\w-]/g, "");
}
```

---

## 📌 Mask credit card number

```js
function maskCard(num) {
  return num.slice(-4).padStart(num.length, "*");
}
```

---

# 🎉 **COMPLETE STRING SUMMARY**

You now understand:

✔ Creation
✔ Immutability
✔ Indexing
✔ Searching
✔ Extracting
✔ Replacing
✔ Trimming
✔ Padding
✔ Splitting
✔ Looping
✔ Unicode handling
✔ Regex methods
✔ Real-world usage

This is every important string concept used in interviews, frontend, backend, APIs, form validation, and daily JS coding.

